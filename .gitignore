# Env files (all contain credentials — copy from .env.example)
backend/.env
backend/.env.qa
backend/.env.prod

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Real database seed (contains production data — generate your own or use the sample)
database/seed.sql

# macOS
.DS_Store

# Docker
.docker/
